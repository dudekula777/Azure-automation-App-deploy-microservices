apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: microservices-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /$1
spec:
  rules:
  - host: 108.143.82.252.nip.io
    http:
      paths:
      - path: /appointment(/|$)(.*)
        pathType: Prefix
        backend:
          service:
            name: appointment-service
            port:
              number: 80
      - path: /patient(/|$)(.*)
        pathType: Prefix
        backend:
          service:
            name: patient-service
            port:
              number: 80
